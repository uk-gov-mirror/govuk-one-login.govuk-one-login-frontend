{
  "name": "logger",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/logger/src",
  "// targets": "to see all targets run: nx show project logger --web",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "packages/logger/src/**/*.{js,ts,json}"
        ]
      }
    }
  }
}